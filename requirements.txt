# SatQuery EvidenceSwarm (SIH26167) - CPU / Orchestration Requirements
# Non-GPU Phase 1 Dependencies

# Core Geospatial & Imaging
numpy>=1.26.0
rasterio>=1.3.9
pyproj>=3.6.1
tifffile>=2024.2.12
pillow>=10.2.0

# API Backend & Server
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9

# Testing & QA
pytest>=8.0.0
requests>=2.31.0

# GPU / VQA Fine-tuning (Phase 2 - RTX 5060 8GB)
torch>=2.2.0
transformers>=4.48.0
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.0
trl>=0.11.0
datasets>=3.0.0
# Fallback if Unsloth unavailable on Windows (uses PEFT fallback)
# unsloth  # optional - Linux only

# Output Renderer & PDF Generation
fpdf2>=2.8.0

# Optional / Future phase integrations (CPU-side)
# sentence-transformers>=2.5.0
# opencv-python-headless>=4.9.0

